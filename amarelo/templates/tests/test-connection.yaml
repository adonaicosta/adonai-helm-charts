apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amarelo.fullname" . }}-test-connection"
  labels:
    {{- include "amarelo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amarelo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
