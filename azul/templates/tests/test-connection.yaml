apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "azul.fullname" . }}-test-connection"
  labels:
    {{- include "azul.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "azul.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
