apiVersion: v1
kind: Service
metadata:
  name: {{ include "azul.fullname" . }}
  labels:
    {{- include "azul.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "azul.selectorLabels" . | nindent 4 }}
